import styled from 'styled-components';

export const ButtonCta = styled.button`
 background:var(--cta) ;
 color:var(--white);
 font-weight: bolder;
 border:2px solid var(--cta);
 border-radius:4px;
 margin: 0.5rem 1rem;
 cursor: pointer;
 padding:6px 8px;
`;
